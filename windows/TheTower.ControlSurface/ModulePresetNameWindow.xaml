<Window x:Class="TheTower.ControlSurface.ModulePresetNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Module preset" Width="470" Height="310"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#0D1420" Foreground="#EDF2F7">
  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="ExplanationText" TextWrapping="Wrap"
               Foreground="#AAB8CB" Margin="0,0,0,12" />
    <TextBlock Grid.Row="1" Text="Display name" FontWeight="SemiBold" />
    <TextBox Grid.Row="2" x:Name="DisplayNameBox" Margin="0,4,0,10"
             AutomationProperties.Name="Module preset display name" />
    <TextBlock Grid.Row="3" Text="Safe stable ID" FontWeight="SemiBold" />
    <TextBox Grid.Row="4" x:Name="PresetIdBox" Margin="0,4,0,3"
             AutomationProperties.Name="Module preset stable ID" />
    <StackPanel Grid.Row="5">
      <TextBlock Text="Use 3–48 lowercase letters, digits, or underscores; start with a letter. IDs are immutable and must be new."
                 Foreground="#93A4BB" FontSize="10" TextWrapping="Wrap" />
      <TextBlock x:Name="ValidationText" Foreground="#FF7187"
                 FontSize="11" TextWrapping="Wrap" Margin="0,8,0,0" />
    </StackPanel>
    <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancel" IsCancel="True" MinWidth="86" />
      <Button Content="Create preset" IsDefault="True" MinWidth="112"
              Margin="8,0,0,0" Click="Create_Click" />
    </StackPanel>
  </Grid>
</Window>
